import { BorderRoundAndShadowDirective } from './border-round-and-shadow.directive';

describe('BorderRoundAndShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderRoundAndShadowDirective();
    expect(directive).toBeTruthy();
  });
});
