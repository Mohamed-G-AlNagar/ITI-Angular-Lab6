import { CreaditCardFormatPipe } from './creadit-card-format.pipe';

describe('CreaditCardFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CreaditCardFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
